[build]
  command = "npm run build"
  publish = "dist"
  base = "/"

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["bcryptjs"]

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
