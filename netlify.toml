[build]
  command = "npm run build"
  publish = "dist"
  base = "/"

[context.staging]
  command = "npm run build"

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["bcryptjs"]

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
